# v4 QA / v4 质量检查

- Atlas validator: pass / 图集验证：通过
- Errors: 0 / 错误：0
- Warnings: 0 / 警告：0
- Format: transparent lossless WebP / 格式：带透明通道的无损 WebP
- Dimensions: `1536 × 1872`
- Grid: `8 × 9`
- Cell size: `192 × 208`
- Sprite schema: v1 (`spriteVersionNumber: 1`)
- Atlas bytes: `364036`
- Atlas SHA-256: `FA8CB52C10674697D3AC7552B184686D7DD3B6B9514B56E119659222AB1F631D`

`contact-sheet.png` is the labeled visual inspection sheet. `working-preview.gif` compares the task-oriented `idle`, `waiting`, `running`, and `review` loops. `validation.json` is the sanitized deterministic atlas-validator output; its file path is repository-relative.

`contact-sheet.png` 是带标签的目视检查总览图。`working-preview.gif` 集中展示面向任务的 `idle`、`waiting`、`running` 与 `review` 循环。`validation.json` 是已移除本机路径的确定性图集验证结果，其中的文件路径为仓库相对路径。
