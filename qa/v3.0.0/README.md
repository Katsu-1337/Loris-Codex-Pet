# v3 QA / v3 质量检查

- Atlas validator: pass / 图集验证：通过
- Errors: 0 / 错误：0
- Warnings: 0 / 警告：0
- Format: transparent WebP / 格式：带透明通道的 WebP
- Dimensions: `1536 × 1872`
- Grid: `8 × 9`
- Sprite schema: v1

`contact-sheet.png` is a labeled visual inspection sheet. `validation.json` is the deterministic validator output.

`contact-sheet.png` 是带标签的目视检查联系表，`validation.json` 是确定性验证器输出。

